#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin>>t;  //t is test cases  could be erased at later stage as the info about it is now given
    int n,c;
    cin>>n>>c;
    int array[n];
    for(int i=0 ; i< n ; ++i ){
        cin>>array[i];
    }
}